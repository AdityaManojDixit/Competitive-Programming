#include<iostream>
#include<vector>
#include<iomanip>
#include<set>
using namespace std;
#define ll long long
#define sum_odd(n) ((n+1)/2*(n/2))
#define sum_even(n) ((n/2)*((n/2)+1))
#define sum_n(n) ((n)*(n+1)/2)
#define sum_neven(n) ((n)*(n+1))
#define sum_nodd(n) (n)*(n)
#define nearestMultiple(a,b) (a+ (b-(a&b))) // Returns the nearest multiple of b > a when a > b
 
int main(){
    
}
